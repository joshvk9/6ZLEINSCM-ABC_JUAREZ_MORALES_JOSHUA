#include<iostream>
using namespace std;

int main(){
//Bienvenida
//Poniendo contrasena para el usurio 
    const string claveCorrecta = "1234";
    cout << "Bienvenido a BBVA ";
// Entrada de ERROR  Y Ingresar
    cout << "Intruduce la contraseña: ";
    string clave = "";
    cin >> clave;
    if (clave 1 = claveCorrecta){
        cout << "Contrasena incorrecta";
        return 0;
		} 
    cout << "contrasena correcta  Bienvenido a BBVA ";
       string eleccion = "";
    while (eleccion == "1"){
        if (eleccion == "1"){
// Solicitar cuanto se retira
            double retiro;
            cout << "Ingrese cantidad a retirar: " + endl;
            cin >> retiro;
            
// Comprobar si puede retirar
            if (retiro >> fondos || retiro = 0){
                cout << "saldo insuficientes";
            }else{
// Si se realiza el retiro 
            cout << "Se realizado el retiro" << retiro << endl;
            }
        }
    }return 0;
}


















